# 爬壁机器人仿真项目总指导规范

## 1. 文档目的

本文档是本项目的总指导规范，用于统一后续仿真环境、机器人模型、传感器、定位融合、全覆盖路径规划、轨迹跟踪控制、测试与验收的目标和实现边界。

后续设计和实现原则上应遵循本文档。若实际开发中需要改变关键目标、系统边界或验收标准，应先更新本文档，再修改代码和配置。

文档职责划分如下：

- `README.md`：项目入口、构建和快速启动；
- `docs/ARCHITECTURE.md`：当前包结构、依赖方向和配置归属；
- `docs/INTERFACES.md`：已实现的 ROS 2 接口、参数和 TF；
- `docs/STATUS.md`：阶段状态、review 闭环和未决事项；
- `results/README.md`：实验结果的用途、有效性和再生成方法。

上述文档描述当前实现，不得修改或降低本规范定义的目标和验收标准；发生冲突时以本规范为准。

## 2. 项目目标

在 Gazebo Sim 中建立一个受真实重力作用的垂直壁面爬壁机器人仿真系统，使机器人能够：

1. 通过持续法向吸附力保持在垂直墙面上；
2. 使用前部两个独立主动轮实现差分驱动，后部使用一个随动轮支撑；
3. 在运动过程中表现出重力导致的横向侧滑，尤其是机器人按水平方向行驶时产生向下漂移；
4. 使用轮式里程计、IMU 和模拟全站仪绝对位置进行位姿估计；
5. 根据用户在壁面上设定的矩形或等腰梯形工作区域，自动生成弓字形全覆盖扫描轨迹；
6. 使用自定义轨迹规划和跟踪算法完成扫描，不依赖 Nav2；
7. 通过位置与航向闭环自动产生向上的航向补偿，使机器人本体可以略微向上偏转，而实际运动轨迹尽量保持为直线；
8. 完成全部扫描后停止，后续可扩展为返回任务起点。

本项目重点验证垂直壁面上的运动、侧滑、定位融合、覆盖路径生成和直线轨迹跟踪，不研究磁吸或负压吸附装置内部机理，也不研究吸附失效过程。

## 3. 已确认的项目边界

### 3.1 本项目包含

- 垂直平面墙体和矩形/等腰梯形任务区域；
- 标准世界重力；
- 恒定、持续、指向墙面的法向吸附力；
- 轮墙接触、摩擦及运动时的轮胎横向滑移；
- 差分驱动机器人动力学；
- IMU、轮式里程计、模拟全站仪和 Gazebo 真值；
- 多传感器位姿融合；
- 矩形区域的弓字形覆盖路径生成；
- 原地转向和分段直线轨迹跟踪；
- 路径、融合轨迹和真值轨迹记录与误差评价；
- RViz 和 Gazebo 中的状态与轨迹可视化。

### 3.2 第一阶段不包含

- Nav2；
- SLAM 和未知环境建图；
- 动态避障和复杂障碍物绕行；
- 磁场、负压腔体、密封泄漏或吸力随距离变化的高保真模型；
- 吸附开启、关闭、脱附和坠落控制逻辑；
- 曲面墙体以及墙面之间的跨面运动；
- 为提高速度而进行的圆弧转弯或路径拐角平滑。

### 3.3 软件平台与编程语言

项目以 ROS 2 为主体，由 ROS 2 负责节点通信、参数、启动、TF、数据记录和可视化；Gazebo Sim 负责物理、传感器和真值仿真，二者通过 `ros_gz_bridge` 或专用接口连接。

编程语言分工如下：

- **C++ / `rclcpp`**：弓字形覆盖路径生成、轨迹跟踪控制、任务状态机、仿真传感器接口，以及必要的 Gazebo System 插件；
- **Python / `rclpy`**：实验管理、辅助工具和非关键在线节点；
- **Python**：离线数据分析、参数扫描、轨迹绘图和验收报告生成；
- **URDF/Xacro/SDF**：机器人与仿真环境建模；
- **YAML**：机器人、物理、传感器、定位融合、控制和验收参数；
- **ROS 2 Python launch**：完整系统的启动与编排。

核心规划、控制和任务状态机默认使用 C++ 实现。Python 不进入对确定性和运行频率要求较高的关键底层控制闭环，除非测试证明其性能满足要求并在本文档中记录例外。

### 3.4 ROS 2 包结构与依赖边界

当前工作区采用以下包结构：

```text
climbot_interfaces   覆盖任务消息和执行 Action
        ↑                          ↑
climbot_coverage              climbot_control
        │                          │
        └──────> climbot_description <──────┘
                         ↑
                  climbot_gazebo
```

`climbot_interfaces` 只定义 ROS 接口，不依赖描述、规划、控制或仿真包。依赖必须
从下游指向公共接口和 `climbot_description`，不得反向依赖。规划器和控制器不得
依赖 `climbot_gazebo`，不得读取 Gazebo 真值、WheelSlip、吸附施力或出生位姿。

配置归属必须遵循：

- 机器人几何、质量、轮系、驱动限幅和规划轮廓属于 `climbot_description`；
- 墙面工作坐标系、可作业宽高属于 `climbot_description`；
- 摩擦、WheelSlip、吸附力、出生位姿和仿真传感器属于 `climbot_gazebo`；
- 覆盖区域和扫描参数属于 `climbot_coverage`；
- 轨迹跟踪、状态机、安全超时和控制增益属于 `climbot_control`。

第一阶段暂不单独建立 `climbot_bringup`。阶段 E 出现控制器和完整系统组合入口后，
再统一评估 launch 与 `ekf_wall.yaml` 的归属。

## 4. 坐标系约定

### 4.1 世界坐标系

Gazebo 世界保持标准三维坐标系和真实重力：

```text
gravity = (0, 0, -9.81) m/s²
```

不得通过关闭重力或旋转世界重力来伪造墙面运动。

### 4.2 壁面工作坐标系 `wall`

在墙面上建立右手坐标系：

- `u`：墙面水平方向，正方向向右；
- `v`：墙面竖直方向，正方向向上；
- `n`：墙面法向；
- 工作区域、覆盖路径和轨迹误差均在 `(u, v)` 平面内表达；
- `wall` 与世界坐标系之间使用固定变换描述。

机器人二维航向角定义在墙面平面内：

- 航向为 `0` 时沿 `+u`；
- 航向正方向按 `wall` 坐标系右手规则确定；
- 所有角度误差必须归一化到 `[-π, π]`。

### 4.3 ROS 坐标关系

建议使用以下 TF 关系：

```text
world / map
└── wall
    └── odom
        └── base_link
            ├── imu_link
            ├── left_wheel_link
            ├── right_wheel_link
            └── caster_link
```

具体发布者可根据定位融合方案调整，但必须满足：

- TF 树唯一、连续、无环；
- `odom` 不得在每段路径开始时重置；
- 分段相对位移通过记录该段起始位姿计算，而不是改变全局坐标系。

**`base_link` 的原点定义为两个主动轮轴的中点在轮墙接触平面上的投影。** 差分驱动的瞬时旋转中心就是该点，因此：

- 原地转向在运动学上不移动 `base_link`，路点仍然是路点；
- 由轮速推算的里程计天然就是该点的位姿，其 `frame_id` 名副其实；
- §10.3 的沿轨、横轨误差和 §10.2 的原地转向都在该点上定义。

不得把 `base_link` 放在机体几何中心或质心。若参考点与旋转中心相距 `d`，一次转角为 `Δθ` 的原地转向会使参考点扫过弦长 `2d·sin(Δθ/2)`；实测 `d = 80 mm` 时一次 90° 转向即产生 113 mm 的纯运动学位移，远超 §14.3 规定的 30 mm 路点终点容差。机体、随动轮和传感器均以相对 `base_link` 的偏置表达。

## 5. 仿真环境

### 5.1 墙体

- 墙体为静态刚体；
- 第一阶段使用平整的垂直矩形墙面；
- 墙面尺寸应明显大于默认工作区域，并为机器人边缘安全距离留出余量；
- 墙面必须具有正确的碰撞几何、法向和摩擦参数；
- 视觉模型与碰撞模型应保持一致，复杂纹理不得影响物理接触。

### 5.2 吸附力

- 吸附力作为持续外力施加到机器人机体；
- 方向始终沿墙面法向并指向墙内；
- 保留机器人和所有动态部件的重力；
- 吸附力大小作为可配置参数；
- 初始吸附力应足以保证机器人静止时不下滑、不脱离墙面；
- 第一阶段不根据接触距离、速度或姿态动态改变吸附力。

吸附力可以使用 Gazebo 的持续 wrench 实现。若现成系统无法满足受力点和坐标变换要求，再实现简单的自定义 System 插件，但插件仍只负责恒定法向力。

**实现约束：`ApplyLinkWrench` 把力施加在 link 的【原点】，不是质心。** 因此吸附力的作用位置由承力 link 的 `<pose>` 决定，与 `<inertial><pose>` 无关。当二者恰好重合时这个区别不可见，一旦分离就会静默改变载荷分配——实测把承力 link 原点移到主动轮轴后，后随动轮法向载荷由 `73.33 N` 降至 `0.00 N`，全部转移到两个主动轮。修改机器人坐标系时必须单独验证吸附作用点未被移动，验证方法是重跑法向载荷测试并逐项比对。

吸附力不得只考虑大小，还必须考虑作用位置及其产生的法向载荷分配：

- 吸附合力在墙面上的投影必须位于两个主动轮与后随动轮构成的三角支撑区域内部；
- 吸附合力不得与主动轮轴重合，否则后随动轮法向载荷可能接近零，机器人处于容易翘起的临界状态；
- 若真实机器人有多个磁铁或吸盘，仿真可将恒定总吸附力分配到多个固定作用点；这仍属于第一阶段的恒定法向吸附模型；
- 单点或多点实现都必须保持总吸附力和合力作用位置可配置；
- 不得通过固定关节补偿不合理的吸附力位置。

### 5.3 摩擦与侧滑

静止机器人应由静摩擦保持在墙面上，不能靠关闭重力或固定关节实现。

机器人运动时使用 Gazebo WheelSlip 或等效的各向异性滑移模型：

- 纵向滑移较小，保证主动轮具有有效驱动力；
- 横向滑移非零，使重力能够在运动过程中产生沿墙面向下的侧滑；
- 机器人静止时应基本保持高度；
- 机器人水平运动时，在未启用轨迹纠偏的情况下应出现可测量的向下漂移；
- 滑移参数、轮胎摩擦系数和轮载估计均应参数化。

不得仅通过把摩擦系数调到静摩擦临界值来制造下滑，因为这会导致静止状态不稳定，不能准确表达“静止吸附稳定、运动时发生横向蠕滑”的目标现象。

## 6. 机器人模型

### 6.1 总体结构

机器人采用扁平、低重心的三轮布局：

- 两个差分主动轮布置在机器人中部略靠前的左右两侧，不放在机体最前沿；
- 一个随动轮布置在后部中央；
- 主动轮分别由独立连续转动关节驱动；
- 随动轮不提供驱动力，第一版采用球形随动轮。

俯视墙面的概念布局：

```text
       机器人前进方向
              ↑
       O─────────────O  ← 主动轮轴
    左主动轮       右主动轮
              │
              ●  吸附合力/质心附近
              │
              o  后球形随动轮
```

### 6.2 模型要求

- 机体厚度尽量小，但惯量必须物理合理；
- 质量、质心、惯量、轮径、轮距和轴距全部参数化；
- 碰撞模型保持简单稳定，不使用不必要的高面数网格；
- 质心和吸附合力位于主动轮轴之后、后球形随动轮之前，并落在三点支撑三角形内部；
- 质心尽量靠近墙面，以减小重力因离墙高度产生的倾覆力矩；
- 吸附合力与主动轮轴之间必须保留明确的稳定力臂，不能为了提高主动轮载荷而与主动轮轴重合；
- 主动轮轴线和 DiffDrive 配置必须与墙面运动平面匹配；
- 后随动轮采用球形随动轮或等效的物理稳定简化模型，避免万向脚轮高频摆振影响轨迹；
- 外观应能直观看出机器人前向、左右主动轮和后随动轮。

### 6.3 吸附稳定性与法向载荷分配

设主动轮轴到后球形随动轮的距离为 `L`，吸附合力位于主动轮轴后方 `d`。忽略其他倾覆力矩时，三点接触的近似法向载荷为：

```text
N_rear  = F_s × d / L
N_drive = F_s × (1 - d / L)
```

其中 `F_s` 为总吸附力，`N_rear` 为后球形随动轮载荷，`N_drive` 为两个主动轮载荷之和。

初始设计建议（**静止工况**）：

- `d = 0.25L～0.35L`；
- 两个主动轮合计承担约 `65%～80%` 的法向载荷；
- 后球形随动轮承担约 `20%～35%` 的法向载荷；
- 左右主动轮法向载荷应基本对称。该项仅在航向沿墙面竖直（重力位于机器人纵向对称面内）时成立；航向水平时重力必然产生滚转力矩 `mgh`，左右载荷差为 `2mgh / 轮距`，此时应以该式核对而非要求对称。

上述比例为初始设计目标，最终数值通过 Gazebo 接触力和运动测试标定。

**运动工况的载荷转移与放宽后的判据。** 机器人离墙质心高度 `h` 会使重力产生倾覆力矩 `M_g = mgh`，上行与下行之间的后轮载荷摆幅为：

```text
ΔN = 2 × m × g × h / L
```

该摆幅与吸附力 `F_s` 无关，而 `20%～35%` 区间的绝对宽度正比于 `F_s`。因此要让**所有姿态**都落在该区间，需要 `2mgh/L ≤ 0.15 F_s`；对本项目的参数（`m = 14.5 kg`、`L = 0.33 m`、`F_s = 220 N`）意味着 `h ≤ 3.8 cm`，对该机器人不现实。

据此，法向载荷判据分为两级：

- **静止工况**：后球形随动轮承担 `20%～35%`，左右主动轮基本对称；
- **运动工况**（直行、上下行、制动、原地转向）：三个接触点法向反力必须全部为正，且**最恶劣工况下后球形随动轮不低于 `0.15 F_s`**。

放宽运动工况判据的依据是 `results/` 中 `measure_normal_loads.py` 的实测数据，而非估算。若后续通过加长 `L`、降低 `h` 或提高 `F_s` 使全姿态都能落回 `20%～35%`，应恢复更严格的判据并更新本节。

## 7. 驱动与底层控制

机器人接收标准二维速度指令：

```text
linear.x  = 墙面平面内的前进速度
angular.z = 绕墙面法向的旋转角速度
```

实际 ROS/Gazebo 轴映射应通过机器人自身坐标系完成，不得让上层算法依赖墙体恰好位于某个世界坐标平面。

底层差分驱动需要支持：

- 线速度、角速度限幅；
- 线加速度、角加速度限幅；
- 左右主动轮编码器或关节状态输出；
- 由轮速计算的连续里程计；
- 原地转向；
- 停车时速度归零。

## 8. 传感器与定位

### 8.1 Gazebo 真值

Gazebo 世界位姿作为评估真值，独立记录，不直接作为控制器的无噪声反馈。

### 8.2 模拟全站仪

模拟全站仪由 Gazebo 真值派生，至少输出机器人在工作坐标系中的绝对位置。模型应支持以下可配置项：

- 发布频率；
- 高斯测量噪声；
- 固定或随机延迟；
- 可选丢帧；
- 消息时间戳和坐标系标识。

本项目真机全站仪的基准发布频率为 **12 Hz**；仿真默认使用相同频率。阶段 C 的初始噪声设为位置一倍标准差 5 mm、固定传输延迟 50 ms，均必须通过 launch/YAML 参数可配置，不得将其写死在控制器中。

阶段 C 的 `odom` 为固定墙面工作坐标系：`+X` 沿初始前进方向，`+Y` 向上，`+Z` 离墙。模拟全站仪适配节点必须把 Gazebo 世界位置转换至该坐标系后再发布；原始 Gazebo 真值保留世界坐标，仅供评估。

第一阶段可以持续可见，不实现遮挡判定。全站仪测量不得与真值轨迹使用同一个“零噪声控制接口”。

### 8.3 IMU

IMU 至少提供：

- 角速度；
- 线加速度；
- 可配置噪声和偏置；
- 正确的安装位姿和坐标变换。

Gazebo IMU 若未在 ROS 消息中正确填充姿态协方差，必须经适配节点发布带噪声及非零姿态协方差的 EKF 输入；不得将零协方差的理想仿真姿态直接融合。第一版姿态噪声基准为一倍标准差 0.5°，后续按真机 IMU 的静态和动态标定数据调整。

IMU 输出的是角速度和线加速度，不将其错误描述为直接输出角加速度。墙面航向估计可融合陀螺仪、重力方向及外部位置约束，但动态加速度对重力观测的污染需要通过融合算法处理。

### 8.4 轮式里程计

- 由主动轮编码器或关节状态推算；
- 连续累计，不在路点或边缘处清零；
- 允许因横向侧滑而与真实位置产生偏差；
- 不得直接使用 Gazebo 真值伪装成轮式里程计。
- 爬壁工况的前向速度与绕墙面法向角速度协方差必须显著大于同编码器的平地初值，并作为可配置参数。其不确定度应覆盖附着力变化、重力诱发微滑、轮墙接触顺从性和吸附力变化；原始编码器里程计与协方差适配后的 EKF 输入必须保留为独立话题。

上述“不得清零”适用于本项目有全站仪绝对定位的主控制链。没有全站仪、只依靠
里程计和边缘检测的简化模式，可以在检测到边缘后重置该扫描行的**局部行程计数**，
但不得重置全局 `odom`、EKF 状态或 TF；该简化模式不属于第一阶段主控制器范围。

### 8.5 状态估计

融合目标是输出机器人在墙面工作坐标系中的连续位姿和速度，输入包括：

- 模拟全站仪绝对位置；
- IMU；
- 轮式里程计。

第一版不从零编写 EKF，优先使用 ROS 2 `robot_localization` 包的 `ekf_node`：

- 使用完整三维状态配置，不启用默认面向地面移动机器人的 `two_d_mode`；
- 所有测量必须具有正确时间戳、协方差和 TF；
- 模拟全站仪只融合其实际提供的绝对位置分量，不伪造航向或速度；
- 轮式里程计主要提供轮子可观测的前向速度和绕墙面法向的角运动，不伪造横向速度观测；
- IMU 根据实际模型融合姿态、角速度和经过验证的线加速度分量；
- Gazebo 真值只用于测试评价，不输入 EKF；
- 通过传感器坐标变换和配置选择，使滤波状态与 `wall` 工作坐标系含义一致；
- 为避免重复使用同一信息，不同时融合由同一轮编码器推导出的高度相关位姿和速度分量，除非验证其协方差处理正确。

当前 EKF 默认以 `50 Hz` 输出融合位姿，控制器也以 `50 Hz` 为第一版运行频率。
全站仪的 `12 Hz` 是绝对位置校正频率；两个全站仪样本之间由 IMU 和轮式里程计
维持高频状态估计。高频 EKF 输出不等价于高频独立绝对测量，控制增益必须结合
测量协方差和延迟设置，不能因输出频率较高而使用过激的横轨位置增益。

`robot_localization` 的配置文件、输入输出话题、状态分量选择、协方差和 TF 发布职责必须纳入版本控制。模拟全站仪若不能直接提供 `ekf_node` 支持的消息类型，应增加适配节点，将其转换为带时间戳、坐标系和协方差的标准 ROS 2 位姿或里程计消息。

采用现成 EKF 的目的，是先验证传感器—定位—规划—控制完整链路，而不是把滤波器设计本身作为第一阶段研究内容。

只有在测试证明 `robot_localization` 无法稳定表达任意壁面方向、墙面运动约束或本项目传感器模型时，才启动专用 EKF 的设计。专用滤波器的候选墙面二维状态为：

```text
x = [u, v, yaw, velocity_u, velocity_v, yaw_rate]ᵀ
```

切换到专用 EKF 前必须保留失败数据，明确现成模块不满足的指标，并更新本文档及接口设计；不得仅因调参尚未完成就重复实现滤波器。

## 9. 工作区域与弓字形覆盖路径生成

### 9.1 输入

第一阶段工作区域为 `odom` 墙面坐标系内的轴对齐矩形或等腰梯形，区域底边默认与墙底边平行。输入至少包括：

- 区域类型：矩形或等腰梯形；
- 矩形的左下角 A、右上角 B；
- 等腰梯形在 A、B 基础上增加右下角 C；
- 扫描方向；
- 检测有效宽度，以及沿行进方向的有效检测长度；
- 相邻扫描带重叠率，或直接指定道间距；
- 机器人轮廓尺寸；
- 边缘安全距离；
- 任务起始角点或自动选择策略。

输入既支持 YAML/ROS 参数，也支持 RViz `Publish Point` 鼠标点选。矩形依次点击 A（左下）、B（右上）；等腰梯形依次点击 A（左下）、B（右上）、C（右下）。鼠标点必须投影到墙面工作平面，规划只使用墙面二维坐标。

矩形的其余两点由 A、B 直接补全。等腰梯形的两个底边点若存在高度偏差，使用平均高度修正：

```text
y_bottom = (A.y + C.y) / 2
A' = (A.x, y_bottom)
C' = (C.x, y_bottom)
center_x = (A.x + C.x) / 2
D = (2 × center_x - B.x, B.y)    左上点
```

由此得到顺序为 A'（左下）、C'（右下）、B（右上）、D（左上）的等腰梯形。必须检查底边宽度、高度、上底宽度、面积和顶点顺序；内缩后区域消失时拒绝规划。上下底宽度在容差内相等时允许视为矩形。

### 9.2 有效区域

规划前应根据机器人外形和安全距离将工作区域向内收缩。内缩采用四条边沿内法向平移后求相邻交点的方法，不得只修改轴对齐包围盒。机器人中心轨迹不得直接落在原始墙面边界上；等腰梯形内缩后仍应保持上下底平行和左右对称。

必须区分两个用途不同的区域：

- `coverage_region`：需要完成检测覆盖的区域；
- `motion_region`：根据墙面物理边界、机器人轮廓和安全距离得到的机器人中心允许运动区域。

`SCAN` 的覆盖率对 `coverage_region` 计算；转向下滑和 `TRANSITION` 可以离开
`coverage_region`，但任何实际或参考轨迹都不得离开 `motion_region`。竖向扫描
底部的转向点到 `motion_region` 下边界必须保留转向下滑余量，第一版使用
`turn_clearance = 0.06 m`；不足时上移转向点或拒绝任务，不得依赖控制器越界补救。

推荐道间距计算方式：

```text
row_spacing = detection_width × (1 - overlap_ratio)
```

若用户直接指定道间距，则仍需验证其不大于允许的最大道间距。

### 9.3 输出路径

路径生成器输出按执行顺序排列的墙面二维路点和线段：

```text
扫描直线 → 原地转向 → 换道直线 → 原地转向 → 反向扫描直线
```

要求：

- 同时支持横向和纵向两种扫描方向；
- 扫描线与选定方向平行；
- 相邻扫描方向相反；
- 每条扫描线端点由扫描直线与有效区域边界求交得到；
- 扫描中心线位于均匀覆盖带的中心，区域边界各保留半个实际道间距由检测有效宽度覆盖；末道不得形成过窄的遗漏带，扫描间距应在不超过设定最大值的前提下均匀分配；
- 所有转向均在路点处完成；
- 第一阶段不做圆角、不切弯、不用曲线连接相邻线段；
- 路点顺序、路径总长度和预计覆盖范围可视化并可导出。

规划器发布的是名义覆盖路径。阶段 E 执行时，控制器允许在每次转向稳定后使用
EKF 实际位置重新生成当前 `TRANSITION` 的直线起点和方向；动态执行参考仍必须由
直线段组成，并单独发布供 RViz 和评价使用，不能静默修改名义 `/coverage/path`。

执行层必须能够区分以下线段语义：

- `SCAN`：有效检测扫描段，采集开启，直线精度要求最高；
- `TRANSITION`：换道和转向下坠补偿段，采集关闭；
- `RETURN`：任务结束后的返回段，采集关闭。

现有 `nav_msgs/Path` 只用于通用显示。第一版权威任务接口冻结为
`climbot_interfaces/msg/CoverageTask`，将路点、线段类型、覆盖区域、运动区域和
检测足迹放在同一条消息中原子发布；不得把 RViz Marker 当作控制输入。具体字段、
校验规则、QoS 和执行 Action 以 [docs/INTERFACES.md](docs/INTERFACES.md) 为准。

## 10. 自定义轨迹规划与跟踪控制

### 10.1 总体原则

不接入 Nav2。覆盖路径被分解为多个两点直线运动，每段采用：

```text
停止 → 原地对准目标线段 → 直线闭环跟踪 → 到达并停止 → 下一段
```

机器人不得在当前线段尚未完成时提前转向下一段。方向改变必须主要发生在路点处的原地旋转阶段。

实际物理系统受扰动时不可能保证数学意义上的绝对零曲率，但控制目标是使每个执行段的真值轨迹尽量贴合规划直线，避免明显弧线、切角和蛇形振荡。

控制优先级固定为：

```text
安全 > 空间轨迹与终点验收 > 时间要求
```

“空间轨迹是硬要求”指必须满足 §14.3 的误差阈值，而不是声称物理系统能达到数学
意义的零误差；时间允许为满足安全和空间误差而延长。

横向为主和竖向为主的弓字形路径使用同一个线段控制器。控制器只根据任意线段
`A → B` 的单位切向、法向和段类型计算误差，不为横扫、竖扫分别编写控制分支；
扫描方向只影响规划器生成的线段顺序、段类型和重力前馈大小。

### 10.2 原地转向

- 转向时线速度严格设为零；
- 使用三角形或梯形角速度轨迹满足角速度和角加速度限制；
- 由目标航向轨迹与实际融合航向的误差闭环修正角速度；
- 航向误差进入容差并稳定后，才允许进入直线跟踪状态。

### 10.3 直线段几何误差

对线段 `A → B`，定义：

```text
t = (B - A) / |B - A|       线段单位方向
n = (-t_v, t_u)             线段单位法向
```

机器人当前位置为 `P`，则：

```text
along_track = (P - A) · t
cross_track = (P - A) · n
remaining   = |B - A| - along_track
```

控制器必须至少使用：

- 沿轨进度误差控制线速度；
- 横轨误差修正目标航向；
- 航向误差控制角速度；
- 终点距离判断当前线段是否完成。

### 10.4 重力侧滑补偿

直线跟踪采用串级控制：

```text
横轨位置误差
      ↓
外环 PI/PID
      ↓
目标航向修正量
      ↓
目标线段航向 + 修正量
      ↓
航向内环 PID
      ↓
角速度指令
```

当重力在当前线段法向上存在分量时，外环应自动生成向上的目标航向偏角。允许机器人机体朝向与规划线段方向存在小角度稳态偏差，只要其实际位置轨迹保持在规划直线附近。重力补偿必须按重力方向在当前线段法向上的投影统一计算：水平段最大，竖直段接近零，斜向段连续过渡，不按“横扫/竖扫”写两套算法。

可选增加基于已标定下滑速度的航向前馈，但闭环横轨误差必须保留，不能只使用固定补偿角。当前水平自然下降/前进比为 `10.56%`，对应的第一版水平向上航向前馈约为 `atan(0.1056) ≈ 6.0°`；该值只作为初值，并随线段方向投影、速度和横轨闭环修正。

阶段 E 的第一版实现把三类限幅分开：重力前馈最大 `±8°`，横轨 PI 反馈最大
`±8°`，两者相加后的最终航向修正仍受 `±12°` 总硬限幅保护。横轨初值为
`Kp = 1.0 rad/m`、`Ki = 0.30 rad/(m·s)`，积分状态限制为 `±0.10 m·s`；
仅在直线前进时积分，定位超时清零，并在反馈或总修正饱和且积分会加深饱和时
停止积分。前馈自身达到独立限幅时仍允许 PI 消除剩余横轨误差。

### 10.5 时间参数化轨迹

保留原方案中的三角形或梯形速度曲线，用于生成每个控制时刻的理论速度和理论沿轨位置。该方法属于“时间参数化轨迹加位置反馈”，可以继续使用，但必须增加以下保护：

- 空间直线轨迹是硬要求，时间轨迹只是尽量满足的软要求；
- 线段完成由实际终点位置、航向和停车状态共同判定，不由预定时间单独判定；
- 速度和加速度输出限幅；
- PID 积分限幅和抗饱和；
- 限制参考位置领先实际位置的最大距离；
- 定位延迟或打滑严重时，不允许参考轨迹无限向前推进；
- 预定时间结束不等于线段完成；
- 当时间轨迹结束但机器人尚未到达时，进入低速终点收敛状态；
- 只有实际位置进入终点容差并且速度足够低，才切换下一段。

### 10.6 保持轨迹直线的约束

- 原地转向期间 `v = 0`；
- 航向未对准前不得前进；
- 正常直线段限制最大航向修正角；
- 对角速度和角加速度限幅，避免蛇形振荡；
- 横轨误差变大时降低线速度；
- 临近终点沿当前直线减速；
- 到达并停止后才转向；
- 不使用会主动切弯的跟踪方式；
- 不跨越或跳过中间路点。

第一版区分共享描述中的绝对硬上限和正常覆盖作业限幅：

| 参数 | 正常作业初值 | 绝对硬上限 |
| --- | ---: | ---: |
| 扫描段线速度 | `0.15 m/s` | `0.30 m/s` |
| 换道段线速度 | `0.12～0.15 m/s` | `0.30 m/s` |
| 终点收敛速度 | `0.02～0.04 m/s` | `0.30 m/s` |
| 原地转向角速度 | `0.60 rad/s` | `1.20 rad/s` |
| 直线跟踪角速度 | `0.35 rad/s` | `1.20 rad/s` |
| 线加速度 | `0.20 m/s²` | `0.40 m/s²` |
| 线减速度 | `0.25 m/s²` | `0.40 m/s²` |
| 原地转向角加速度 | `1.00 rad/s²` | `1.50 rad/s²` |
| 直线跟踪角加速度 | `0.80 rad/s²` | `1.50 rad/s²` |
| 重力前馈航向角 | `±8°` | 最终修正仍受总限幅 |
| 横轨 PI 航向角 | `±8°` | 最终修正仍受总限幅 |
| 总航向修正角 | `±12°` | 可配置保护值 |

这些正常作业值是阶段 E 的保守初值。航向补偿初值已通过水平单段实验验证，仍需在
斜向、竖向和完整任务中继续验证；其他数值也必须通过对应单段实验再冻结。控制输出依次经过：

```text
轨迹前馈与反馈 → 速度/航向限幅 → 线/角加速度限制 → 左右轮联合饱和 → cmd_vel
```

轮距为 `b` 时必须同时检查：

```text
v_left  = v - ωb/2          a_left  = a - αb/2
v_right = v + ωb/2          a_right = a + αb/2
```

任一轮速度或加速度超限时，应对左右轮指令按相同比例缩放以保持期望曲率，不得
独立截断单轮。每个控制周期还必须满足 `|Δv| ≤ a_limit·dt` 和
`|Δω| ≤ α_limit·dt`，防止 PID 输出突变造成轮墙反复刮擦和蛇形振荡。
阶段 E 应在控制 YAML 中增加有效轮缘速度和加速度上限；第一版分别使用
`0.30 m/s` 和 `0.40 m/s²`。还必须检查共享描述中的轮关节速度上限
`30 rad/s` 和力矩上限 `80 N·m`，控制层不得修改这些机器人硬件属性。

### 10.7 横向与竖向换道策略

两种扫描方向共用 §10.3～§10.6 的直线控制器，但根据转向下滑相对下一条扫描线
的方向采用不同的简单换道策略。

**横向为主。** 转向下滑垂直于下一条水平 `SCAN`，不能留给扫描段吸收。第一次
转向稳定后，以 EKF 实际位置作为竖直 `TRANSITION` 起点；该换道段终点在下一
水平扫描线上方预留一次预计转向下滑量，使第二次转向后落入下一扫描线容差。
预计量只作前馈，转向后的 EKF 实际位置用于判定是否允许开始采集。

**竖向为主。** 不在扫描终点上方预留空间，不逐列倒车，也不要求第二次转向后
返回名义起点。设当前竖直扫描名义终点为 `P`、下一扫描名义起点为 `Q`：

```text
竖直 SCAN 到 P
→ 原地转向并允许下滑到实测 P1
→ 动态生成并跟踪 P1 到 Q 的斜直线 TRANSITION
→ 原地转向并允许下滑到实测 Q1
→ 直接从 Q1 沿下一条竖直线开始 SCAN
```

`P1 → Q` 是采集关闭的直线，不要求水平。`Q1` 与 `Q` 的主要偏差沿下一条竖直
扫描线，因此直接接受为实际扫描起点，不增加返回 `Q` 的倒车动作。向下扫描可能
由此少覆盖顶部约一次转向下滑长度；不得用机器人中心线是否到达 `Q` 直接判断
漏扫，而应使用检测装置的二维有效足迹计算实际扫过区域。

覆盖模型至少包含垂直于行进方向的 `detection_width` 和沿行进方向的
`detection_length`。若竖向弓字任务的预计或实测覆盖率低于 §14.3 的 `98%`，
任务生成/执行层增加一条采集开启的顶部水平收边直线；不得为每一列增加倒车修正。
收边线及其过渡同样必须位于 `motion_region`。`detection_length` 尚未由真实检测载荷
标定时按保守值处理，不能凭视觉估计宣称已经覆盖。

### 10.8 已实测的扰动量

以下数值来自 `results/` 中的可重复实验，控制器设计必须以它们为输入，而不是重新估算。改变质量、吸附力、质心高度或 WheelSlip 参数后必须重新测量。

**原地转向下滑。** 原地转向时两主动轮反向旋转、轮胎横向刮擦，静摩擦被破坏，机器人沿墙面下滑：

```text
下滑量 ≈ 0.50 mm × |转角(度)|
```

30°～180° 范围内该系数稳定在 `0.48～0.54 mm/度`。**该下滑与转向速度无关**：0.30 rad/s 与 0.60 rad/s 下测得 `0.504` 与 `0.507 mm/度`，位移相差不足 1 mm，尽管耗时相差一倍。因此不能靠提高转向速度减小它。一次 90° 转向下滑约 `45 mm`，已超过 §14.3 规定的 `30 mm` 路点终点容差，弓字形路径每换一道有两次转向。

换道处理必须遵循 §10.7：横向为主时在竖直换道段预留第二次转向下滑量；竖向为主时接受下滑并动态生成斜直线换道，不逐列倒车。§10.2 要求原地转向期间 `v ≡ 0`，因此不得在转向过程中一边转一边补偿。

**仅有航向闭环不足以抑制侧滑。** 四方向实测中，控制器已用融合航向锁定目标航向，水平段仍下沉 `125 mm / 1.200 m`（10.4%）和 `88 mm / 1.116 m`（7.9%）。航向环只约束机器人朝向，不约束其位置；必须按 §10.4 增加横轨位置外环，否则机器人会保持水平朝向被重力横推下去。

**竖直段侧偏很小。** 纯上行、下行的横向偏移仅 `3～5 mm`（行程 1.13～1.22 m），约 0.3%。侧滑补偿的设计目标是水平和斜向段。

### 10.9 参考点必须与旋转中心重合

见 §4.3 的 `base_link` 定义。若跟踪的参考点偏离差分驱动的瞬时旋转中心 `d`，一次转角 `Δθ` 的原地转向会使该点扫过弦长 `2d·sin(Δθ/2)`，这是纯运动学位移，与滑移无关。规划、跟踪、里程计和真值评估必须全部使用同一个参考点。

## 11. 任务状态机

第一阶段至少包含：

```text
IDLE                等待任务
PLAN                生成并验证覆盖路径
GO_TO_START         前往或确认任务起点
ALIGN               原地对准当前线段
TRACK_LINE          跟踪当前直线段
FINAL_APPROACH      低速收敛到当前终点
WAYPOINT_REACHED    停止并切换下一段
MISSION_COMPLETE    完成全部扫描
STOPPED             安全停止
ERROR               故障停车
```

需要处理的异常至少包括：

- 工作区域或参数非法；
- 定位数据超时；
- TF 不可用；
- 控制指令超时；
- 长时间无法到达路点；
- 机器人越出安全区域。

任何影响定位和安全边界判断的严重异常都应使机器人停车。

**速度指令看门狗是强制项，不是可选项。** Gazebo 的 DiffDrive **无限期锁存最后一条速度指令，没有任何内建超时**：实测发布一条 `Twist` 后让发布者退出，机器人仍以恒定速度持续行驶（20 秒内测得 0.131 m/s 匀速）。因此控制器崩溃、被杀或话题中断时，机器人不会停下，而会一路开出墙面边缘并在重力和吸附力作用下自由坠落——本项目已实际发生过一次，机器人飞到距墙面 75 km 处。

要求：

- 系统中必须有一个节点在超过配置时限未收到新速度指令时主动发布零速；超时值可配置；
- 任何会发布 `cmd_vel` 的脚本或节点，都必须在收到 `SIGINT`/`SIGTERM` 时于退出前发布停车指令（Python 的 `finally` 在 `SIGTERM` 下不执行，必须用信号处理器）；
- 使用键盘遥控时需注意：`teleop_twist_keyboard` 仅在按键时发布，看门狗超时值须大于按键自动重复间隔。

## 12. 参数化与可重复性

以下参数不得硬编码在算法中：

- 墙体尺寸和位姿；
- 工作区域；
- 机器人质量、惯量和几何尺寸；
- 轮径、轮距和轴距；
- 吸附力；
- 摩擦和横向/纵向滑移；
- 传感器频率、噪声、延迟和偏置；
- 速度和加速度限制；
- 轨迹间距、检测宽度、检测长度、安全边距和转向安全余量；
- PID 参数和航向修正角限制；
- 各类到达容差、超时和验收阈值。

固定随机种子的测试必须可重复。所有关键实验应保存参数文件、软件版本和结果数据。
正式侧滑标定必须从全新仿真世界开始，防止上一轮的位姿、滤波状态和已行驶距离
污染下一轮；同一次标定内部的三次水平运动应使用融合航向保持，但不得启用横轨
位置纠偏。

## 13. 分阶段实施方案

### 阶段 A：基础物理与机器人模型

1. 建立垂直墙面和标准重力世界；
2. 建立扁平三轮机器人模型；
3. 配置两个前主动轮的差分驱动；
4. 配置后随动轮；
5. 施加恒定法向吸附力；
6. 验证静止吸附、前进、后退、原地转向和停止。

### 阶段 B：运动侧滑模型

1. 配置轮墙摩擦；
2. 配置横向和纵向 WheelSlip；
3. 验证静止时基本不下滑；
4. 禁用轨迹纠偏，测量水平运动时的自然下滑；
5. 标定滑移参数，使现象稳定且可重复。

### 阶段 C：传感器与定位融合

1. 发布轮式里程计；
2. 添加 IMU；
3. 发布带噪声、频率和延迟的模拟全站仪绝对位置；
4. 建立连续 TF；
5. 实现或配置融合定位；
6. 对比轮式里程计、融合位姿和 Gazebo 真值。

### 阶段 D：覆盖路径规划

1. 接收矩形或等腰梯形工作区域和路径参数，支持 YAML 与 RViz 点选；
2. 计算机器人中心的安全有效区域；
3. 自动生成弓字形折线路径；
4. 可视化路点、扫描线、方向和安全边界；
5. 对非法输入给出明确错误。

### 阶段 E：自定义轨迹跟踪

1. 实现任务状态机；
2. 实现原地转向轨迹和航向闭环；
3. 实现直线段时间参数化速度轨迹；
4. 实现沿轨、横轨和航向闭环；
5. 实现参考防超前、限幅、抗饱和和终点收敛；
6. 验证重力侧滑下的自动抬头补偿与直线跟踪。

E3 已完成第 4、5 项的可复用单段核心和参数化节点：它以 EKF `odom` 位姿在
50 Hz 仿真时钟下计算沿轨、横轨与航向误差；水平行驶按已标定的下滑比例生成有限的
抬头前馈，横轨反馈继续收敛残差。航向超出对准阈值时线速度为零，终点按制动距离
减速；输出再经过线/角加速度与左右轮联合速度、加速度限幅。仿真 launch 已把键盘、
实验脚本和该节点统一接到 `/control/cmd_vel`，由速度看门狗唯一输出 Gazebo
`/cmd_vel`；当前一次只允许运行一个上游控制源。

### 阶段 F：系统测试和数据评价

1. 完成单条水平、竖直和斜向直线测试；
2. 分别完成横向为主和竖向为主的完整矩形弓字形覆盖任务；
3. 自动输出轨迹误差和覆盖率；
4. 对比关闭和开启横轨闭环时的下滑差异；
5. 固化启动、测试、绘图和结果导出流程。

## 14. 验收标准

### 14.1 功能验收

以下项目全部满足，才视为第一阶段功能完成：

- 一条命令或一个 launch 文件能够启动完整仿真；
- 墙面、机器人、重力、吸附力和轮墙接触同时生效；
- 机器人使用两个中部略靠前的主动轮差分驱动，后球形随动轮不主动驱动；
- 吸附合力投影位于三点支撑区域内部，且不与主动轮轴重合；
- 静止、直行、制动和原地转向时，三个轮墙接触点均保持正法向载荷，不发生后轮卸载或机体翘起；最恶劣工况下后球形随动轮法向载荷不低于 `0.15 F_s`（见 §6.3）；
- 机器人能够前进、后退、停止和原地旋转；
- 静止状态下机器人不会持续明显下滑或脱离墙面；
- 关闭横轨纠偏后，水平运动能够观察并测量到向下侧滑；
- 开启横轨纠偏后，控制器能够形成向上的航向补偿并显著减小高度漂移；
- 能够发布 Gazebo 真值、IMU、轮式里程计、模拟全站仪和融合位姿；
- 能够根据任意合法矩形或约定的等腰梯形区域生成横向、纵向弓字形路径；
- 能够按“原地转向＋直线运动”的方式执行完整覆盖任务；
- 任务执行中不重置全局里程计；
- 定位丢失或严重故障时机器人能够停车；
- 完成任务后机器人停止，并输出任务结果。

### 14.2 路径几何验收

- 所有名义路径点和动态执行参考均位于 `motion_region` 内；
- 扫描线与指定扫描方向一致且互相平行；
- 相邻扫描线间距满足配置误差要求；
- 相邻扫描方向交替；
- 不生成圆弧拐角或跨路点捷径；
- 路径覆盖指定的有效矩形或等腰梯形区域；
- 动态斜向 `TRANSITION` 和顶部收边扫描仍由直线段组成；
- 同一输入参数应生成完全一致的路径。

### 14.3 轨迹跟踪验收

每条直线段均需基于 Gazebo 真值计算：

- 横轨误差 RMS；
- 最大绝对横轨误差；
- 终点位置误差；
- 转向结束航向误差；
- 实际轨迹长度与规划长度之比；
- 水平段高度漂移；
- 最大航向补偿角和角速度；
- 是否出现切角或明显蛇形振荡。

第一版建议使用以下默认验收阈值，待机器人实际尺寸和真实数据确定后再冻结最终数值：

| 指标 | 建议初始阈值 |
| --- | ---: |
| 单段横轨误差 RMS | ≤ 20 mm |
| 单段最大绝对横轨误差 | ≤ 50 mm |
| 路点终点位置误差 | ≤ 30 mm |
| 原地转向结束航向误差 | ≤ 2° |
| 完整水平扫描段净高度误差 | ≤ 30 mm |
| 相邻扫描线间距误差 | ≤ 20 mm |
| 任务期间漏扫面积比例 | ≤ 2% |
| 定位严重超时后的停车响应 | ≤ 0.5 s |

上述阈值属于“建议初值”，不是不可修改的物理要求。最终阈值应依据机器人尺寸、检测载荷有效宽度、全站仪精度和期望作业质量确定。

覆盖率必须按机器人实际执行轨迹扫过的二维检测足迹计算，足迹同时使用
`detection_width` 和 `detection_length`。竖向为主的任务不得通过逐列倒车回到
名义扫描起点；若顶部漏扫导致覆盖率低于 98%，应增加一条顶部水平收边扫描。

### 14.4 侧滑补偿专项验收

在相同初始状态、速度、吸附力和滑移参数下分别执行：

1. 横轨补偿关闭的水平直线运动；
2. 横轨补偿开启的水平直线运动。

要求：

- 补偿关闭时存在稳定、可重复的向下漂移；
- 补偿关闭时三次水平下降/前进比的总体变异系数不大于 5%；
- 补偿开启后，水平段净高度误差相对关闭状态至少降低 70%；
- 补偿开启后机器人允许保持小幅向上航向偏角；
- 补偿过程不能产生明显蛇形振荡；
- 实际位置轨迹应比机器人机体朝向更接近规划水平直线。

### 14.5 定位验收

- 所有传感器消息时间戳有效；
- TF 树连续且无环；
- 融合定位在正常传感器条件下不中断、不跳变；
- 轮式里程计能体现侧滑造成的不可观测误差；
- 模拟全站仪包含配置的频率、噪声和延迟；
- 融合位置误差应显著小于仅使用轮式里程计时的长期误差；
- 评估脚本不得把控制器使用的估计值当作 Gazebo 真值。
- 第一版应能使用 `robot_localization/ekf_node` 完成全站仪、IMU 和轮式里程计融合；
- `robot_localization` 的输入状态选择、协方差、频率和 TF 发布配置必须可追溯；
- 若改用专用 EKF，必须提供与 `robot_localization` 在同一数据集上的定量对比，并证明现成模块未达到已定义指标。

### 14.6 工程质量验收

- 构建无错误；
- 自动化测试通过；
- 启动和停止过程无残留关键进程；
- 参数集中配置并有单位说明；
- 关键节点、话题、服务、动作和 TF 有文档；
- 至少提供一个默认矩形区域的可复现实验；
- 测试结果包含参数、规划路径、融合轨迹、真值轨迹和指标摘要；
- 不使用硬编码绝对路径依赖特定开发机。
- 核心规划、轨迹控制和任务状态机使用 C++，辅助测试与分析工具可使用 Python；
- ROS 2 包依赖、Gazebo 版本、消息接口和启动入口有明确文档。

## 15. 测试场景

至少建立以下测试：

1. **静止吸附测试**：机器人静止在墙面，验证高度和法向距离稳定；
2. **法向载荷测试**：记录三个轮墙接触点的法向力，验证吸附合力位置、主动轮载荷比例和防翘裕量；
3. **水平自然侧滑测试**：禁用横轨补偿，执行水平直线；
4. **水平补偿测试**：启用横轨补偿，重复相同水平直线；
5. **竖直运动测试**：分别向上和向下运动，验证驱动和定位；
6. **原地转向测试**：测试多个角度，包括小角度三角形速度轨迹和大角度梯形速度轨迹；
7. **单段直线测试**：测试水平、竖直和斜向线段；
8. **矩形覆盖测试**：分别完成横向为主和竖向为主的完整弓字形路径，验证竖向斜线换道、无逐列倒车和可选顶部收边扫描；
9. **定位噪声测试**：改变全站仪噪声和频率；
10. **定位超时测试**：暂停绝对位置输入，验证安全停车或降级策略；
11. **参数重复性测试**：固定随机种子重复运行，验证结果一致性。

阶段 C 必须提供可重复的滑移标定脚本：使用 Gazebo 真值只作评估，记录静止高度变化、无横轨纠偏且融合航向保持时的水平下降/前进比，以及融合航向保持条件下等时上行、下行速度。每项运动测试至少重复三次，输出均值和水平下降比总体变异系数；标定脚本的速度必须按仿真时间计算，并拒绝超过配置重复性阈值的实验。

## 16. 数据记录与结果展示

每次正式实验至少记录：

- 规划工作区域和安全有效区域；
- 规划路点和线段；
- Gazebo 真值位姿；
- 轮式里程计；
- IMU；
- 模拟全站仪；
- 融合位姿；
- 速度指令；
- 目标航向、实际航向和航向补偿量；
- 总吸附力及三个轮墙接触点的法向力；
- 沿轨误差、横轨误差和终点误差；
- 当前任务状态。

结果图至少包含：

- 规划轨迹、融合轨迹和真值轨迹叠加图；
- 横轨误差随时间曲线；
- 水平段高度误差随时间曲线；
- 目标与实际航向曲线；
- 线速度和角速度曲线；
- 补偿开启/关闭对比；
- 覆盖区域和漏扫区域示意。

## 17. 开发原则

- 先完成简单、稳定、可测量的基础模型，再提高物理和传感器复杂度；
- 机器人不得通过固定关节连接墙面；
- 不关闭重力；
- 不用 Gazebo 真值替代轮式里程计或无噪声全站仪参与正常控制；
- 规划、控制和评价使用统一的墙面工作坐标系；
- 保持模块职责单一，规划器不直接控制车轮，控制器不负责生成工作区域；
- 所有关键参数可配置；
- 每完成一个阶段均应先测试，再进入下一阶段；
- 任何声称完成的功能都必须有可重复的测试或数据支持。

## 18. 后续可扩展方向

完成本规范的第一阶段后，可按需要扩展：

- 梯形和任意凸多边形工作区域；
- 墙面障碍物与局部绕行；
- 全站仪遮挡、丢锁与重定位；
- 自适应侧滑估计和航向前馈；
- 不同墙面材质对应的滑移参数地图；
- 相机检测覆盖模型和图像姿态校正；
- 任务中断、恢复与返回起点；
- 曲面结构和跨墙面运动；
- Nav2 或更通用的任务规划框架。

这些扩展不得妨碍第一阶段“垂直墙面、真实重力、持续吸附、运动侧滑、矩形/等腰梯形弓字覆盖和直线段闭环跟踪”的核心目标。
