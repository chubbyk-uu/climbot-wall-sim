# 爬壁机器人仿真：项目规范

更新：2026-09-01。本文件是产品目标、范围和不可违背约束的唯一规范来源；运行命令见
[README](README.md)，接口字段见 [接口合同](docs/INTERFACES.md)，逐项通过状态见
[验收矩阵](docs/ACCEPTANCE.md)。此前完整的设计推导与阶段记录见
[规范归档](docs/archive/specifications/PROJECT_GUIDE_2026-08-27.md)。

## 目标

在 ROS 2 Jazzy / Gazebo Harmonic 中实现一台在垂直平面墙上工作的差分驱动爬壁机器人，并闭合：
物理与定位、覆盖规划、受控执行、位置触发巡检、不可变原图归档、离线预处理与墙面拼接。
仿真结论不得冒充实机安全或性能认证。

## 范围

| 包含 | 当前不包含 |
| --- | --- |
| 平面垂直墙、重力、持续吸附、三点接触与侧滑 | 实机驱动器、硬件急停、真实摩擦/吸附认证 |
| 矩形与等腰梯形的横/纵弓字覆盖 | Nav2、自由曲面、动态障碍与多机器人协同 |
| 全站仪、IMU、轮式里程计、EKF 与独立仿真真值评价 | 真全站仪时钟、相机自动对焦、在线增量拼接 |
| G1–G4 采集、P1 预处理、P2 平面硬切拼接 | 用 Gazebo 真值或墙面 DDS 参与拼接优化 |

## 不可违背的工程约束

1. `odom` 是墙面工作坐标，原点在墙左下；`x` 向右、`y` 向上、`z` 指向机器人。
   Gazebo `world` 真值只能被传感器仿真和独立评价器读取，不能进入控制或拼接算法。
2. 所有自动运动只能经 `/control/cmd_vel` 和速度看门狗到 `/cmd_vel`；命令超时、异常或取消
   必须发布零速。执行期间不允许多个上游控制源竞争。
3. 规划后的机器人中心路径必须完全位于安全工作区；相机覆盖足迹不是可走区，也不能替代
   覆盖验收。
4. 任务、原图、标签、相机标定和 processed-run 一经发布即不可改写。每个后续阶段使用新的
   独立输出目录，并记录输入哈希、参数、软件版本和结果哈希。
5. 外部大数据默认写入当前用户的 `$HOME/climbot_data`；可由 `CLIMBOT_DATA_ROOT` 显式覆盖。
   公共文档、代码注释、提交的 JSON 和日志不得记录用户目录或主机绝对路径。
6. pose-only 与 optimized 拼接必须使用相同帧、网格、分辨率、插值和 hard-cut 像素归属；
   唯一变量是位姿图修正。不得用融合平均掩盖几何误差。
7. 诊断墙 DDS/feature 真值仅用于拼接完成后的评价。低可信、孤立帧、无覆盖像素和失败边
   必须显式输出，不得静默丢弃或视觉填充。

## 质量与安全原则

- 物理：吸附投影保持在三点支撑内；重力侧滑必须可复现，补偿效果必须以独立轨迹量测。
- 定位：控制与照片标签使用同一 EKF；`precision` 与 `realistic` 是可追溯配置，系统残差
  不得通过伪造协方差掩盖。
- 规划：任务输入、实际中心路径和相机足迹分别表示；转向、入轨和换道不得计入扫描覆盖。
- 采集：位置触发和曝光位姿绑定异步完成；保存数必须与冻结参考一致，异常采集触发受控取消。
- 拼接：候选、接受/拒绝边、连通分量、优化残差、不确定度与资源使用均须可复查；并发不得
  改变业务结果。

## 验收与阶段状态

A–G4、P1 和 P2.1–P2.7e 已有仿真实现与证据。**P2-06 已于 2026-09-01 关闭**：五次重复采集在
冻结任务的巡检域内均零漏拍，绝对毫米与细节保持门限已由三组带渲染记录的采集推导、并由一组
完全未参与推导的数据独立检验通过，人工 100% 全分辨率复核完成且未发现错位类缺陷。复核另发现
optimized 母版上可见亮度接缝，已量化且在门限内，作为平场标定优化项另记。完整状态、证据和
后续工作只在以下页面维护：

- [当前状态](docs/STATUS.md)；
- [验收矩阵](docs/ACCEPTANCE.md)；
- [拼接设计与 P2-06 关闭条件](docs/MOSAIC_PLAN.md)；
- [正式结果索引](results/README.md)。

规范改变必须同时更新其唯一归属文档和验收矩阵；历史推导只追加到归档，不覆盖已发布证据。
