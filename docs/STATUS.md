# 当前状态

更新：2026-09-01。这里仅保留当前阶段、有效结论和下一步；逐项判据见
[验收矩阵](ACCEPTANCE.md)，历史过程见[状态归档](archive/status-history/STATUS_2026-08-27.md)
及各专项计划归档。

## 项目快照

| 范围 | 状态 | 当前结论 |
| --- | --- | --- |
| A–E：模型、定位、规划、控制 | 仿真完成 | 垂直墙物理、侧滑、融合定位、矩形/梯形覆盖、暂停恢复与故障停车均有回归证据 |
| F：系统验收 | 仿真完成 | 仿真内要求已闭合；实机门限、硬件急停和驱动失效关闭仍待真实硬件 |
| G1–G4：巡检采集 | 完成 | 位置触发、曝光位姿绑定、平场标定、不可变原图归档和 RViz 操作已闭合 |
| P1：离线预处理 | 完成 | 归档校验、平场/暗场、可选去噪和去畸变发布到独立 processed-run |
| P2：离线拼接 | 仿真关闭 | hard-cut 拼接、分阶段 provenance、绝对真值、接缝指标和人工原尺寸复核均通过 |

“完成”只表示当前仿真实现和证据闭合，不等同于实机产品认证。

## P2-06 关闭结论

五组独立采集使用同一冻结任务和诊断墙，每组横向 680 + 纵向 660，共 1,340 帧。第 2、3、4
组用于推导门限，第 5 组在门限提交后采集、只用于独立检验；第 1 组因缺少渲染工况记录只作
旁证。五组冻结巡检域内 21 个 feature 均零漏拍。

| 指标 | 冻结门限 | 第 5 组检验 |
| --- | ---: | ---: |
| 巡检域内 feature 漏拍 | `= 0` | `0` |
| 绝对锚点偏差 P95 | `≤ 2.5 mm` | `1.130 mm` |
| 局部残差 P95 | `≤ 0.5 mm` | `0.143 mm` |
| 尺度误差绝对值 | `≤ 50 ppm` | `16.2 ppm` |
| 航向误差绝对值 | `≤ 0.01°` | `0.0002°` |
| 接缝 / off-seam P95 | `≤ 5.0` | `3.50` |
| optimized 不劣于 pose-only | 必须成立 | 通过 |

人工已浏览 170 个全分辨率 tile，未发现重影、拖影或“几何覆盖但视觉错误”。域外约 43 万
feature 像素按产品定义不作门禁要求；这不表示它们物理上不可拍到。完整的巡检域、足迹分类、
门限来源、已知 `+21 ppm` 尺度偏置和适用范围见[拼接设计](MOSAIC_PLAN.md)，可引用结果见
[结果索引](../results/README.md)。

## 最近关闭的可靠性与光度问题

- **大图偶发等待**：根因是 Fast DDS 默认共享内存段无法容纳一帧全部分片。三个大图发布者
  已使用 64 MiB profile；46,300 次压测零慢捕获，完整纵向任务 `660/660`。分层证据与以后
  的诊断规则见[长任务时序稳定性](LOCALIZATION_GAP_PLAN.md)。
- **平场欠校正**：旧灰板与诊断墙使用不同 Gazebo 着色路径。标定靶现与被摄墙使用相同 PBR
  材质、同一表面和均匀 albedo；新 30 帧标定使角/中心从 `0.907` 改善到 `1.011`，同一输入
  的 optimized 接缝 P95 从 `7` 降到 `6 DN`，几何指标未退化。以后新数据使用
  `flat_field_sim_pbr_exp065_20260901.npz`，其文件名与 SHA-256 已记录在
  `climbot_image_processing/config/inspection_flat_field.yaml`；SHA-256 对不上就拒绝处理，
  少给或给错都必须显式覆盖；既有正式摘要保留其原始标定 provenance，不改写。
  排查中被否定的两条也记在世界模型里，免得后人重走：旧灰板重拍与 2026-08-25 的存档
  逐点重合（角/中心 `0.673` vs `0.672`），标定并未过期，错的是靶面材质；把靶从
  `0.059 m` 挪到被摄平面只让角/中心从 `0.673` 变到 `0.668`，站位只值半个点。反过来，
  只换 PBR 路径而不带 `albedo_map` 比原来的经典材质更差——角/中心 `0.709`，并因 PBR
  镜面项引入 `0.737` / `0.839` 的左右不对称。起作用的是 albedo 贴图本身。
- **高并发测试竞态**：此前 `-j8` 暴露的失败均按真实缺陷修复并补回归；默认保持 8 并发，
  不以降低并发或重跑通过掩盖低频失败。当前完整套件为 1,342 tests 全绿。

## 当前性能专项

离线图像链已开始 CUDA 迁移，实施与验收合同见
[GPU 加速计划](GPU_ACCELERATION_PLAN.md)。第一步 CPU 解码复用基线 `3b72f44` 已完成：同一
1,340 帧 P2-06 fusion 从 `101.99 s` 降到 `88.81 s`，两遍 PNG 解码未命中从 `57,145` 降到
`15,730`，十个正式产物 SHA-256 逐个不变。CUDA 实现尚未进入当前接口，默认仍为 CPU。

## 下一步

当前没有未关闭的仿真阶段门禁。继续工作的优先顺序是：

1. 按 [GPU 加速计划](GPU_ACCELERATION_PLAN.md)完成 backend 隔离、fusion CUDA、P1 CUDA 与
   数值/性能/故障验收；
2. 进入实机前冻结硬件急停、吸附/摩擦、定位和轨迹门限，并在目标系统验证时序上界；
3. 新墙面、新任务几何或实机数据到来后，按 P2-06 同一 provenance 与独立检验口径重新验收，
   不把当前单墙面结果外推为普遍性证明；
4. 仅在有新测量证据时继续优化残余 `+21 ppm` 尺度偏置；它当前不是功能阻塞项。

## 数据与文档边界

- 正式结果只放 `results/`；诊断日志和临时 A/B 产物放 `/tmp`。
- 外部数据根、保留链和可清理项以 [DATA_RETENTION](DATA_RETENTION.md) 为准。
- 环境、安装和快速启动见[根 README](../README.md)；实验变体与故障处置见
  [OPERATION](OPERATION.md)；接口和配置归属见 [INTERFACES](INTERFACES.md)。
