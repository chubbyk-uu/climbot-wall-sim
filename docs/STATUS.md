# 实施状态与待办

更新日期：2026-08-13。

本文档只记录当前实现状态。目标、算法约束和验收阈值以
[PROJECT_GUIDE.md](../PROJECT_GUIDE.md) 为准。

## 阶段状态

| 规范阶段 | 对应当前称呼 | 状态 | 说明 |
| --- | --- | --- | --- |
| A：基础物理与机器人模型 | 阶段 1～3 的模型部分 | 已完成 | 墙面、真实重力、三轮模型、持续吸附、差分驱动和键盘控制 |
| B：运动侧滑模型 | 阶段 1～3 的侧滑部分 | 已完成 | 静止稳定；水平下降比均值 10.56%，三重复 CV 0.64%；下行比上行快 7.89% |
| C：传感器与定位融合 | 阶段 4 | 已完成 | 12 Hz 全站仪、IMU、轮式里程计、EKF、TF 和四方向评价 |
| D：覆盖路径规划 | 阶段 5 / P4 | 已完成 | 矩形/等腰梯形、横纵扫描、RViz 点选、安全内缩和节点级测试 |
| E：自定义轨迹跟踪 | 当前阶段 | 进行中 | 已完成任务接口、单段控制、E7 多段 Action 和转向下坠动态换道；竖向完整任务与最终评价待完成 |
| F：系统测试和数据评价 | 最终联调 | 未开始 | 完整覆盖执行、误差与覆盖率报告 |

“已完成”表示当前阶段实现和已有自动化测试通过，不代表整个第一阶段项目已经
通过 §14 的最终系统验收。

## P4 review 闭环

| 项目 | 状态 | 结果 |
| --- | --- | --- |
| 拆出 `climbot_description` | 已完成 | 仿真和规划共享机器人/墙面描述，规划器不再依赖仿真配置 |
| `detection_width + overlap_ratio` | 已完成 | `row_spacing = width × (1-overlap)` |
| footprint 推导安全边距 | 已完成 | 从共享 `robot.yaml` 注入并按全向转动包络内缩 |
| `/coverage/path` 段航向 | 已完成 | 每个位姿指向下一段，末点沿用到达航向 |
| 规划失败发布空 Path | 已完成 | 失败、清空和开始新区块时均清除旧任务 |
| 墙面可视化读取共享尺寸 | 已完成 | 宽高来自 `climbot_description/config/wall.yaml` |
| 覆盖率、确定性、节点级测试 | 已完成 | 几何覆盖率 ≥98%，同输入完全一致，包含成功/失败 launch 测试 |
| 侧滑 CSV 重复采样 | 已关闭 | 正式基线 10209 行，同一 `phase + time_s` 0 重复 |
| 侧滑标定重复性 | 已关闭 | 水平段加入融合航向保持但不做横轨纠偏；三重复 CV 0.64% ≤ 5% |
| 后球轮“脱离” | 已关闭（误判） | 1000 Hz 原生和事件式测量均为 100% 接触，最低 37.8 N |

相关提交：`11726ec`、`206c211`、`41704d6`、`510d051`，以及本轮侧滑基线提交。

## 2026-08-13 全量 review 闭环

| 项目 | 状态 | 结果 |
| --- | --- | --- |
| B-1 控制包类型 | 已完成 | 明确导出 `ament_cmake`，隔离构建后 ROS 可发现，launch 测试通过 |
| B-2 定位超时 | 已完成 | `0.25 s` 未收到有效 EKF 位姿即持续零速，并清空历史指令 |
| B-3 速度命令路由 | 已完成 | 看门狗是 `/cmd_vel` 唯一发布者；键盘、实验和控制统一走 `/control/cmd_vel` |
| C-1 角点校验 | 已完成 | 生产规划函数拒绝非法 `start_corner` 和非法行距 |
| C-2/C-5 共享物理量 | 已完成 | 规划与控制标准 launch 从 description 注入，直接运行缺参数时安全失败 |
| C-3/C-4 控制输入 | 已完成 | 启动期完整校验、非有限输入拒绝、完整四元数偏航提取 |
| H-3/H-5 模型与卫生 | 已完成 | Xacro 默认值对齐 YAML 并由测试保护；清理维护者、无效形参和 `.gitkeep` |
| G-1 重力补偿预算与 PI | 已完成 | 前馈/PI 各限 `8°`、总限 `12°`；条件积分抗饱和；水平 1.210 m 实测最大横轨偏差 3.6 mm |
| G-2 到达判定 | 已完成 | 二维位置、补偿后航向、融合停车速度和 `0.30 s` 稳定判定；完成锁存零速；0.60 m 实测声明时误差 14.4 mm |
| G-2 ALIGN 稳定判定 | 已完成 | `2°` 内且角速度归零稳定 `0.50 s` 后才直行，`10°/12°` 迟滞重入 |
| G-3 转向速度曲线 | 已完成 | 三角形/梯形曲线进入 C++ 控制库；90° 实测直行前误差 0.86°，转向线速度恒为零 |
| E7 覆盖 Action | 已完成 | `/coverage/execute` 支持不可变任务、多段推进、反馈、取消、定位/控制超时和越界停车；节点级三段执行通过 |
| G-4 动态换道 | 已完成 | 横向预留第二次转向下滑、竖向采用实测斜线且不倒车；Gazebo 横向三段最大横轨误差为 6.45/7.75/14.96 mm，无 >20 mm 可见反向往复 |

修复提交：`690d490`、`68a72dd`、`ccf7ca5`、`452bf55`、`babd771`、`1404220`、
`849a1e2`、`1a266ec`，以及随后单独提交的 G-2 到达判定实现。

## 当前未决事项

### 1. WheelSlip 法向载荷局限

Gazebo WheelSlip 按配置的标称法向力缩放柔度，不随三个接触点的瞬时载荷变化。
这是已知模型保真度限制；第一阶段接受该限制，但真机参数解释时必须保留差异。

### 2. 集成 launch 和定位配置归属

`coverage_sim.launch.py` 和 `ekf_wall.yaml` 目前仍放在各自阶段包中。待完整任务状态机
和统一系统入口出现后，再决定是否新增 `climbot_bringup` 并统一外移。

## 下一步顺序

1. 补充斜向、竖向单段和不同初始横轨误差的 G-1 回归工况；
2. 执行竖向小区域完整任务，验证斜向动态换道和不倒车策略；
3. 将 Gazebo 真值横轨包络、航向摆幅和曲率评价固化成可重复脚本；
4. 进入阶段 F 的完整覆盖验收。
